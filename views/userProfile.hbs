<div class="row">
  <div class="col-md-4 userInfo">
    {{!--Lefthand section with userinfo and avatar image--}}
    <div class="row">
      <div class="col-md-12">
        <img src={{username.avatar}} alt="User image/avatar" id="avatarPic"/>{{!--User image URL from user table, needs handlebars and knex--}}
      </div>

    </div>
    <div class="row">
      <div class="col-md-12 profileInfo">
        <h2>Profile Info</h2>
        <ul>
          <li>{{username.first_name}}</li>{{!--First name from user table, needs handlebars and knex--}}
          <li>{{username.last_name}}</li>{{!--Last name from user table, needs handlebars and knex--}}
          <li>{{username.userAge}}</li>{{!--Age from user table, needs handlebars and knex--}}
          <li>{{username.aboutMe}}</li>{{!--aboutMe from user table, needs handlebars and knex--}}
        </ul>
        <a href="/editProfile"><button>Edit Profile</button></a>
      </div>

    </div>
    <div class="row">
      <div class="col-md-12">
        <a href="/addBee"><button>Add Bee</button></a>
      </div>
    </div>
  </div>

  <div class="col-md-4 beeFeed">
    {{!--Middle section with bee feed, needs to prepend for added bees--}}
    <div class="row">{{!--Need to do a FOR EACH based off of bees added by the logged in user--}}
      <div class="col-md-12">
        <h2>Bee Feed</h2>
        <ul>
          <li><img src="/images/megachile_apicalis.jpg" alt="a pic here" class="beeFeedPost"/>Bee Post</li>
          <li><img src="/images/megachile_apicalis.jpg" alt="a pic here" class="beeFeedPost"/>Bee Post</li>
          <li><img src="/images/megachile_apicalis.jpg" alt="a pic here" class="beeFeedPost"/>Bee Post</li>
          <li><img src="/images/megachile_apicalis.jpg" alt="a pic here" class="beeFeedPost"/>Bee Post</li>
        </ul>
      </div>

    </div>
  </div>

  <div class="col-md-4 beeFacts">
    <div class="row">
      <div class="col-md-12">
        <h3>Bee Fact of the Day</h3>
      </div>
    </div>
    <div class="row" id="beeFact">
      <div class="col-md-12">
        <img src="/images/megachile_apicalis.jpg" alt="Image for bee fact of the day" id="beeFactImage"/>
        <p></p>
      </div>
    </div>
    <script>
      console.log("entrance")
      var randomFact = new Promise(function(resolve, reject){
        $.get('../beefact', function(data){
          console.log("you did something")
          return data;
        }).then(function(data){
          console.log("you did something2")
          var factArray = [];
          data.forEach(function(data){
            factArray.push(data);
            console.log("you did something3")
          })
          var randomized = factArray[Math.floor(Math.random() * factArray.length)];
          console.log(randomized);
          for (var i=0; i<randomized.length; i++){
            var factHeading = '<h4>'+ randomized[i].heading + '</h4>';
            var factImage = '<img src="' randomized[i].image + '/>';
            var factText = '<p>' + randomized[i].fact + '</p>';
            $("#beeFact").append(factHeading);
            console.log("you did something")
          }
        })
      })
    </script>
    <div class="row">
      <div class="col-md-12">
      </div>
    </div>
</div>
</div>
